# Iteration Notes

## Goal
Verify and harden mobile/touch drawing on the whiteboard: with touch input (Playwright mobile/touch emulation), drawing with the pen/highlighter tools must produce real strokes (unified pointer events exist — confirm pointerType touch works end-to-end and fix any bugs), two-finger pan and pinch-zoom gestures must work, the md:hidden mobile toolbar must let a user select tool/color/size and draw, and any breakage found must be fixed with a regression test. Add mobile-emulated e2e coverage (extend the existing Playwright setup in frontend/e2e/) proving a touch draw lands on the canvas, and keep npm run typecheck + npm test green.

## State
- **Iteration 1 (touch gestures):** Fixed the two-finger pan/pinch gesture in `WhiteboardCanvas.vue` — it previously entered "pan mode" (enablePan/disablePan + Konva draggable) but NEVER moved the viewport, so two-finger gestures were dead on touch. Added `computePinchViewport()` + `toStagePoint()` in `composables/useViewport.ts` + 5 unit tests. `handlePointerUp` calls `setViewport(...)` once to sync the final viewport.
- **Iteration 2 (mobile-touch e2e + real mobile-toolbar bug fix):**
  - Added `frontend/e2e/mobile-touch.spec.ts` — 3 tests on a `hasTouch/isMobile` 390×844 viewport that dispatch real `PointerEvent`s (`pointerType: 'touch'`, `pointerId` held constant) on the Konva stage content element:
    1. Mobile toolbar selects pen (collapsed strip) → a one-finger touch stroke lands on the canvas (`canvasFingerprint` changes).
    2. Two-finger pan moves the viewport **without committing a stroke** — pan by (+40,+30) changes the fingerprint, then pan back by the exact inverse RESTORES the baseline fingerprint (proves no element was committed).
    3. Mobile toolbar color (hex input `#ff0000` → header readout `#FF0000`) and size (button `8` → active class) flow through to a touch stroke.
  - **Real bug found & fixed:** the `md:hidden` mobile toolbar was rendered BELOW the visible viewport in mobile emulation (`fixed bottom-0` anchors to the LAYOUT viewport, whose `innerHeight` is 970 vs the 844 visual viewport), so `<main>` (and Nuxt DevTools) intercepted taps — the toolbar was unclickable. Fix: `WhiteboardToolbar.vue` mobile bar `fixed` → `absolute bottom-0`, anchored by adding `relative` to the `flex-1 flex overflow-hidden` wrapper in `pages/whiteboard/[id].vue`, plus root `h-screen` → `h-dvh` (canonical mobile-height fix).
  - **E2E infra fix:** `playwright.config.ts` now runs the Nuxt webServer with `TEST=1` so `@nuxt/devtools` bails in module setup — its floating widget used to swallow clicks on the bottom toolbar (absent in prod, so purely a dev/e2e artifact).
  - Verified this session by booting the real stack (:8002 Laravel + :3000 Nuxt + :3001 WS relay): **full e2e suite 5/5 green** (smoke + collab mouse-sync + the 3 mobile-touch tests), `npm run typecheck` clean, `npm test` 399/399.
- **Iteration 3 (gesture-interrupt cancels the in-progress stroke):**
  - **Real bug fixed:** when finger 1 started drawing and finger 2 landed (gesture), the partial stroke was NOT cleanly cancelled — with ≥2 points it was **committed** at finger-2 lift (stray shape after a pan); with 1 point it was dropped but the remote active-stroke broadcast was **never ended** (peers saw a stuck preview forever), and `currentPointerType`/pressure stayed dirty.
  - **Fix:** added a `cancel()` hook to `ToolHandler` + `dispatchCancel` in `useToolHandlers.ts` (falls back to the tool's existing `deactivate()`). Pen + highlighter implement `cancel()`: resets stroke state and calls the new `cancelActiveStroke(strokeId)` in `useCollaborativeCanvas.ts` (deletes from `yActiveStrokes` + clears throttle state, NO element push) so collaborators' preview clears. `WhiteboardCanvas.handlePointerDown`: on the 2nd finger, if `isDrawing`, `dispatchCancel(currentTool)` + reset `isDrawing`/pressure/pointerType before entering gesture mode. Wired the new `cancel-active-stroke` prop through `pages/whiteboard/[id].vue`. Shape tools (rect/line/arrow/…) get cancellation for free via their existing `deactivate()`.
  - **Tests:** `useToolHandlers.test.ts` (+3 dispatchCancel), `usePenTool.test.ts` + `useHighlighterTool.test.ts` (+2 each: cancel aborts the active stroke without committing), `useCollaborativeCanvas.test.ts` (+1: cancelActiveStroke removes from yActiveStrokes without adding to yElements, contrast with endActiveStroke). New e2e regression test in `mobile-touch.spec.ts`: "a two-finger gesture started mid-stroke cancels the partial stroke" — finger 1 draws a partial stroke, finger 2 lands, pure-pan (+40,+30), then inverse pan MUST restore the baseline fingerprint (a committed stroke would leave pixels no pan can undo).
  - Verified: **full e2e suite 6/6 green** (smoke + collab + 4 mobile-touch), `npm run typecheck` clean, `npm test` 407/407.
- **Iteration 4 (pinch-zoom e2e):** Added a 5th mobile-touch e2e test — "two-finger pinch-zoom zooms the viewport without committing a stroke". Two fingers at ±60px pinch OUT to ±120px (finger distance 120→240, so `computePinchViewport` yields exactly 2× zoom; fingerprint changes → the pinch engaged), then pinch back in by the exact inverse (distance 240→120, zoom 2×→1.0×) which restores the baseline fingerprint (no element committed). The gesture math uses container-relative px against a FIXED `startDistance`, so the round-trip is exactly reversible; the mid-gesture wobble from sequentially-dispatched finger moves (f1 solo move ⇒ distance 180, zoom 1.5) is harmless because the final frame is always `currentDistance/startDistance`.
- **Iteration 5 (highlighter translucency bug — `globalAlpha` is a Konva no-op):**
  - Added a 6th mobile-touch e2e test: "mobile toolbar selects the highlighter; a touch highlight renders as a translucent stroke". Selects `Highlighter (B)` from the collapsed mobile strip, draws a touch stroke that must land (fingerprint changes), then a new `pixelAt` helper samples the stroke's midpoint RGBA from the layer canvas and asserts it's a mid-gray blend (black at 50% over the #f5f5f5 background), with a parallel pen stroke asserted opaque black as the contrast.
  - **Real bug found & fixed:** the test FAILED (sampled pixel was `0,0,0` = opaque black) because Konva 9.3.15 has **no `globalAlpha` node property** — only the `opacity` attr is read (`SceneContext._applyOpacity` sets `context.globalAlpha = shape.getAbsoluteOpacity()`); a `globalAlpha` key in a shape config is stored as an ignored attr. So highlighter strokes had ALWAYS rendered fully opaque (and the in-progress previews at 0.7 were opaque too). Fixed in `WhiteboardCanvas.vue`: `globalAlpha` → `opacity` in all three stroke configs — `buildStrokeConfig` (committed element, line ~2645), `getActiveStrokeConfig` (remote preview, line ~2681), `currentStrokeConfig` (local preview, line ~3282).
  - Verified: full e2e suite **8/8 green** (smoke + collab + 6 mobile-touch), `npm run typecheck` clean, `npm test` 407/407. Mouse path (collab.spec) unaffected.
- **Iteration 6 (mouse-path highlighter translucency regression test):** Added a 7th e2e test in `collab.spec.ts` — "desktop mouse highlighter renders translucent; pen renders opaque (regression)". It draws a committed highlighter stroke over the MOUSE path (desktop toolbar `Highlighter tool, press B`), polls the canvas fingerprint to confirm the stroke landed, then samples the stroke midpoint via the new shared `pixelAt` helper (copied into collab.spec) and asserts mid-gray (60..200 per channel); a parallel pen stroke sampled at its own midpoint must be opaque black (<80). This closes the gap that only the TOUCH path asserted translucency after the `globalAlpha`→`opacity` fix in Iteration 5 — the bug predated touch and affected mouse-drawn highlights equally. `selectHighlighter` helper added alongside `selectPen`. First attempt failed with a background pixel (245 = #f5f5f5): the midpoint was sampled immediately after mouse-up before the stroke committed/rendered — fixed by polling `canvasFingerprint` to differ from baseline before sampling (same pattern the touch spec already used).
- **Iteration 7 (remote active-stroke preview clears on cancel — 2-browser touch e2e):** Added an 8th mobile-touch e2e (10th e2e overall) — "a remote touch stroke preview appears on a peer and clears when the drawer cancels into a gesture". Two touch browser contexts (390×844, hasTouch, isMobile) both logged in as the SAME owner. Flow: owner selects pen via the md:hidden toolbar, finger-1 down + moves (no pointerup) → poll the PEER's `canvasFingerprint` ≠ baseline (the in-progress stroke renders as a live remote preview over the WS relay, before any commit) → finger 2 lands and both fingers lift with NO movement (a pure cancel — viewport unchanged, so fingerprint math holds) → poll peer AND owner fingerprints == baseline (the `cancelActiveStroke` broadcast cleared the peer's preview) → 750ms settle re-check both still at baseline (cancelled, not committed). Closes the last gap from Iteration 3: `cancelActiveStroke` was only UNIT-tested; now the peer-side preview clearing is guarded end-to-end over the real relay. Works with two sessions of the same account because `[id].vue` derives `currentUser.id` as a RANDOM per-page `user-${Math.random()}` — the `strokeId.startsWith(userId)` self-filter in `yActiveStrokes.observe` only excludes the drawer's OWN tab.
- **Iteration 8 (shared e2e helper module):** Extracted the duplicated helpers out of `collab.spec.ts` + `mobile-touch.spec.ts` into a single `frontend/e2e/helpers.ts`: `login`, `createWhiteboard`, `waitForConnected`, `canvasFingerprint`, `pixelAt`, `canvasBox`, `touchPointer`, `touchStroke`, `expectCanvasToChange` (the last was collab-only but is a generic poll+settle assertion, so it moved too). Both specs now import from it; `mobile-touch.spec.ts` gained two tiny local `selectMobilePen`/`selectMobileHighlighter` wrappers + a `waitForCanvas` alias to cut the repeated `toHaveClass(/bg-blue-100/)` dance. No behavior changed — pure refactor. Spec-local helpers that stayed put are genuinely spec-specific: `createShareLink`/`selectPen`/`selectHighlighter`/`drawStroke` (collab/mouse path) and the inline mobile-toolbar expansion (color/size test). The new `TouchEvent` type is exported from helpers for anyone dispatching multi-pointer sequences. Verified: **full e2e suite 10/10 green** (smoke + collab + 8 mobile-touch), `npm run typecheck` clean, `npm test` 407/407.
- **Next:** lower-priority ideas — a 2-browser cancel-during-PINCH variant (this iteration proved the viewport can return to baseline over the relay; pure-cancel avoids fragile inverse-gesture math), or sweep for remaining duplicated assertions in the spec files now that the helper module exists. The zoom-percentage readout idea is moot: no zoom readout exists in the UI (`zoomPercent` in useViewport is exported but never rendered).
- **Gotchas:**
  - The loop gate is only `npm run typecheck && npm test` (unit); e2e is NOT auto-run by the gate. This session I re-used the already-running stack (:8002 Laravel + :3000 Nuxt with `TEST=1` + :3001 WS relay) and verified the suite by hand — e2e must be run separately.
  - In Playwright mobile emulation the LAYOUT viewport (`window.innerHeight` = 970) is taller than the visual viewport (844); `position: fixed; bottom: 0` resolves against the layout viewport → off-screen UI. Anchor mobile bars inside the app frame with `absolute` + a `relative` wrapper (and use `h-dvh`), not `fixed`.
  - `@nuxt/devtools`' floating widget intercepts pointer events at the bottom of the viewport during e2e; run the Nuxt dev server with `TEST=1` (see playwright.config.ts). `reuseExistingServer: true` means a stale manually-started server without `TEST=1` will be reused — restart it when devtools appears.
  - Gesture math must use stage-relative coords (via `toStagePoint`), not raw `clientX/clientY`, or pinch anchor drifts by the container's page offset.
  - `setViewportDirect` does NOT sync; call `setViewport` once on gesture end (kept — don't sync per-move or you spam the relay).
  - Konva bakes the viewport transform into the layer-canvas PIXELS (no CSS transform), so pan/zoom changes `canvasFingerprint`; the two-finger tests exploit pan-back-to-baseline to prove "no stroke committed".
  - A regression e2e that draws-then-gestures must use a PURE pan (both fingers translate by the SAME delta) so the inverse pan restores the exact viewport — a mixed pan+pinch changes zoom and the inverse translation won't land back on the baseline fingerprint.
- Konva 9.x has NO `globalAlpha` node property — a `globalAlpha` key in a shape config is silently ignored; only `opacity` is read (it sets `context.globalAlpha = getAbsoluteOpacity()`). Any transparency on Konva shapes must use `opacity`. (This is how highlighter strokes rendered opaque for so long.)
- To assert rendered translucency in e2e, sample the layer canvas pixel at the stroke midpoint (map CSS → canvas px via `canvas.width/rect.width`); the highlighter's black@0.5 over #f5f5f5 lands ~(122,122,122) vs the pen's opaque (0,0,0).
  - Pinch-zoom e2e: to get a deterministic target zoom, the finger DISTANCE ratio is what matters (zoom = currentDistance/startDistance against the fixed start). Start at ±60px and pinch OUT to ±120px = exactly 2×; the inverse returns to exactly 1.0×. The pinch must be pure (fixed centroid, fingers spread along the same axis) so the inverse gesture lands back on the baseline viewport/fingerprint.
  - In any stroke e2e that samples pixels, poll `canvasFingerprint` (≠ baseline) AFTER mouse-up/touch-up BEFORE calling `pixelAt` — sampling immediately can hit the background while the stroke still commits/renders.

## Context (from prior code review — read before changing code)

### Current touch/mobile state
- **Unified pointer events already exist** in `frontend/components/whiteboard/WhiteboardCanvas.vue`: the stage root binds `@pointerdown`/`@pointermove`/`@pointerup`/`@pointerleave`/`@pointercancel` → `handlePointerDown/Move/Up/Leave/Cancel`. `currentPointerType` tracks `'mouse' | 'pen' | 'touch'` (set from `evt.pointerType`). There is gesture state for two-finger pan (comment at ~line 1709) and `getStagePositionFromEvent` converts client coords to stage space. Touch pressure is defaulted to 0.5 when unsupported (see the pressure extraction ~line 2214).
- **Mobile toolbar** exists in `frontend/components/whiteboard/WhiteboardToolbar.vue` (the `md:hidden` bottom bar) with `handleMobileToolSelect`/`handleMobileColorSelect`/`handleMobileSizeSelect` and a `primaryTools` list (~line 698). `frontend/pages/whiteboard/[id].vue` wires toolbar → canvas via `@select-tool` etc.
- **E2E infra exists**: `frontend/playwright.config.ts` boots the full stack (Laravel :8002 + Nuxt :3000 + WS relay :3001), `frontend/e2e/global-setup.ts` seeds an approved owner, `frontend/e2e/collab.spec.ts` proves two-browser live sync using a mouse draw + a `getImageData` canvas fingerprint. `frontend/e2e/smoke.spec.ts` covers login/create/reload.

### What to verify (and fix if broken) with touch emulation
1. **Touch draw end-to-end**: on a mobile viewport with `hasTouch: true, isMobile: true`, a pen stroke (pointerdown → pointermove → pointerup with `pointerType: 'touch'`) must produce a rendered stroke element (same canvas-fingerprint / Konva-node assertion the collab spec uses).
2. **Toolbar on mobile**: the `md:hidden` bottom bar must render and let the user pick the pen tool, a color, and a size, then draw.
3. **Two-finger gestures**: two-pointer pan and pinch-zoom must not fight touch-drawing (a single finger draws, two fingers pan/zoom). Verify the existing gesture code actually engages for touch pointers; fix if it doesn't.
4. **Coords under touch**: `getStagePositionFromEvent` must map touch client coords correctly (Konva layers are scaled; check the draw lands where the finger is — use the fingerprint + a coordinate probe).
5. Fix any breakage found with a regression test (unit and/or e2e). Do NOT break the existing mouse path (collab.spec must stay green).

### How to emulate touch in Playwright
- `test.use({ viewport: { width: 390, height: 844 }, hasTouch: true, isMobile: true })`.
- Playwright's `page.touchscreen.tap()` is limited; Konva relies on **Pointer Events**, so the most reliable simulation is dispatching real pointer events with `pointerType: 'touch'` on the stage root, e.g. `page.locator('.whiteboard-container').dispatchEvent('pointerdown', { pointerId: 1, pointerType: 'touch', clientX, clientY, isPrimary: true, pressure: 0.5 })` followed by pointermove/pointerup with `pointerId` held constant. Multi-touch = two `pointerId`s (pointerId 1 + 2) for the pinch/pan gesture.
- Reuse the existing fingerprint helper approach from `collab.spec.ts` to assert a stroke rendered (getImageData-based, since headless Chromium serves a stale surface to drawImage).

### Verification (do end-to-end before DONE)
1. `cd frontend && npm run typecheck && npm test` green (currently 394 tests).
2. `npm run test:e2e` green: existing smoke + collab (mouse) still pass, plus the new mobile/touch spec.
3. If you fixed canvas/toolbar code, the touch draw must visibly land on screen and the mouse path must be unaffected.
4. `php artisan test` only if Laravel touched (shouldn't be).

### Gotchas
- Do not regress the live-sync path (SYNC_FULL/SYNC_DELTA, reconnect, presence) — those are covered and must stay green.
- The e2e stack already runs on :8002/:3000/:3001; Playwright webServer `reuseExistingServer: true` will reuse running ones.
- `currentPointerType` and pressure defaults matter for stroke rendering (perfect-freehand `getStroke`); a touch stroke with pressure 0.5 is valid.
- Keep changes minimal and inside the canvas/toolbar/e2e files. Do not touch the Goal section. Update the State section every iteration.

## Log

- Iteration 1: two-finger pan/pinch-zoom implemented (was a no-op); `computePinchViewport` in useViewport.ts + 5 unit tests; typecheck + 399 unit tests green.
- Iteration 2: added `frontend/e2e/mobile-touch.spec.ts` (touch draw lands, two-finger pan without a stroke, mobile toolbar tool/color/size). Fixed real mobile bug: mobile toolbar was off-screen (`fixed bottom-0` anchors to the 970px layout viewport, not the 844px visual viewport) — now `absolute` in a `relative` wrapper + root `h-dvh`. Disabled Nuxt DevTools in e2e via `TEST=1` webServer env. Full e2e suite 5/5 green, typecheck clean, 399 unit tests green.
- Iteration 3: added `cancel` hook to the tool system (`dispatchCancel` falls back to `deactivate`); pen/highlighter `cancel()` ends the active-stroke broadcast without committing via new `cancelActiveStroke` in useCollaborativeCanvas; `handlePointerDown` cancels any in-progress drawing when the 2nd finger lands. This fixes a stray partial shape committing at gesture-end and a stuck remote preview. Added unit tests (registry + pen/highlighter + canvas) and a 4th mobile-touch e2e regression (draw→gesture cancels, inverse pan restores baseline). Full e2e 6/6 green, typecheck clean, 407 unit tests green.
- Iteration 4: added a 5th mobile-touch e2e covering pinch-zoom — two-finger pinch out (±60→±120px, exactly 2× zoom) changes the canvas fingerprint, and the exact inverse pinch in restores the baseline fingerprint (zoom fully reversible, no stroke committed). Full e2e 7/7 green (smoke + collab + 5 mobile-touch), typecheck clean, 407 unit tests green.
- Iteration 5: added a 6th mobile-touch e2e covering the highlighter (touch stroke lands AND renders translucent via a new `pixelAt` RGBA sampler). It exposed a real bug: `globalAlpha` is a no-op in Konva 9 (only `opacity` drives context alpha), so highlighter strokes — and the 0.7 previews — rendered opaque. Fixed all 3 stroke configs in WhiteboardCanvas.vue to use `opacity`. Full e2e 8/8 green, typecheck clean, 407 unit tests green.
- Iteration 6: added a 7th e2e in collab.spec.ts asserting the committed highlighter renders translucent on the MOUSE path (the Iteration-5 bug predated touch; only touch had coverage). Midpoint pixel must be mid-gray (60..200), pen contrast opaque (<80); poll the fingerprint after mouse-up before sampling. Full e2e 9/9 green, typecheck clean, 407 unit tests green.
- Iteration 7: added an 8th mobile-touch e2e (10th overall) — a 2-browser regression proving the remote active-stroke preview APPEARS on a peer while the drawer's finger-1 stroke is in flight, then CLEARS when finger 2 lands and cancels it into a gesture (pure cancel, no viewport change). Both touch contexts logged in as the same owner (random per-page userId makes the peer render the drawer's preview). Peer + owner fingerprints return to baseline + 750ms settle. Closes the Iteration-3 cancel's untested peer-facing half. Full e2e 10/10 green, typecheck clean, 407 unit tests green.
- Iteration 8: extracted the duplicated e2e helpers (`login`, `createWhiteboard`, `waitForConnected`, `canvasFingerprint`, `pixelAt`, `canvasBox`, `touchPointer`, `touchStroke`, `expectCanvasToChange`) into `frontend/e2e/helpers.ts` and rewrote `collab.spec.ts` + `mobile-touch.spec.ts` to import them (mobile-touch gained `selectMobilePen`/`selectMobileHighlighter`/`waitForCanvas` wrappers). Pure refactor, no behavior change. Full e2e 10/10 green, typecheck clean, 407 unit tests green.
