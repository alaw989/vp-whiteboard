---
phase: 05-real-time-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composables/useCursors.ts
  - composables/useCollaborativeCanvas.ts
  - components/whiteboard/WhiteboardCanvas.vue
  - components/whiteboard/WhiteboardCursorPointer.vue
autonomous: true

must_haves:
  truths:
    - "Users see remote cursors with colored pointer and name label"
    - "Local cursor position broadcasts to other users in real-time"
    - "Cursors render on topmost layer without interfering with hit detection"
    - "Local user's cursor is NOT rendered (only remote cursors)"
    - "Cursor positions sync via Yjs Awareness API"
  artifacts:
    - path: "composables/useCursors.ts"
      provides: "Cursor management with Awareness API"
      exports: ["useCursors"]
      min_lines: 80
    - path: "components/whiteboard/WhiteboardCursorPointer.vue"
      provides: "Remote cursor visualization component"
      min_lines: 40
  key_links:
    - from: "composables/useCursors.ts"
      to: "wsProvider.awareness"
      via: "provider.awareness property access"
      pattern: "awareness\\.setLocalState|awareness\\.getStates|awareness\\.on\\('change'"
    - from: "components/whiteboard/WhiteboardCanvas.vue"
      to: "composables/useCursors.ts"
      via: "useCursors() composable call in script setup"
      pattern: "useCursors\\("
---

<objective>
Implement Yjs Awareness-based cursor tracking for multi-user collaboration.

Purpose: Enable users to see each other's cursor positions in real-time while working on the whiteboard. The Awareness API provides automatic broadcast, cleanup, and reconnection handling without manual implementation.

Output: Remote cursors render on canvas with colored pointers and name labels.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-real-time-collaboration/05-RESEARCH.md
@/home/deck/Sites/vp-whiteboard/composables/useCollaborativeCanvas.ts
@/home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCanvas.vue
@/home/deck/Sites/vp-whiteboard/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create useCursors composable with Awareness API</name>
  <files>composables/useCursors.ts</files>
  <action>
Create new composable at /home/deck/Sites/vp-whiteboard/composables/useCursors.ts:

1. Accept wsProvider, userId, userName as parameters
2. Access awareness from provider.awareness (NOT from custom yCursors Map)
3. Create currentUser ref with id, name, color from getUserColor(userId)
4. Create remoteCursors ref as Map<number, any> for all other users' cursor states
5. Implement awareness change handler:
   - Call awareness.getStates() to get all users
   - Filter out current user (awareness.clientId)
   - Populate remoteCursors with state.user, state.cursor, state.tool
6. Implement updateLocalCursor(x, y, tool) function:
   - Call awareness.setLocalState({ user, cursor: {x, y}, tool })
   - NOTE: Throttling will be added in 05-04
7. Implement cleanup() function:
   - awareness.off('change', handler)
   - awareness.setLocalState(null) to mark offline
8. Subscribe to awareness.on('change') and call handler immediately for initial sync
9. Export: currentUser, remoteCursors, updateLocalCursor, cleanup, awareness

IMPORTANT: Use the existing getUserColor helper from useCollaborativeCanvas.ts (move to shared utility or duplicate here).
</action>
  <verify>
File /home/deck/Sites/vp-whiteboard/composables/useCursors.ts exists and exports useCursors function with awareness integration.
</verify>
  <done>
useCursors composable initializes with wsProvider, subscribes to awareness changes, and exposes remoteCursors Map for rendering.
</done>
</task>

<task type="auto">
  <name>Create WhiteboardCursorPointer component</name>
  <files>components/whiteboard/WhiteboardCursorPointer.vue</files>
  <action>
Create new component at /home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCursorPointer.vue:

1. Accept single prop: presence of type UserPresence (with id, name, color, cursor)
2. Template uses ClientOnly wrapper to avoid SSR issues
3. Render absolute-positioned div with:
   - Triangle pointer using CSS border technique or SVG
   - Name label with background color matching user.color
   - Transition for smooth position updates
4. Position style: left: ${presence.cursor.x}px, top: ${presence.cursor.y}px
5. Add pointer-events-none class to prevent interfering with canvas clicks
6. Use user.color for both pointer fill and label background
7. Add z-index of 1000 to render above all canvas elements

CSS pointer shape:
- 12px height triangle pointing down-left
- Use border trick: border-left/right/top with transparent/border-bottom-color
</action>
  <verify>
File /home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCursorPointer.vue exists and renders cursor with pointer + name label.
</verify>
  <done>
WhiteboardCursorPointer component renders colored cursor pointer with user name label at cursor position.
</done>
</task>

<task type="auto">
  <name>Integrate useCursors into WhiteboardCanvas</name>
  <files>components/whiteboard/WhiteboardCanvas.vue</files>
  <action>
Modify /home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCanvas.vue:

1. Import useCursors from ~/composables/useCursors
2. In script setup, call useCursors(wsProvider, userId, userName):
   - Destructure: remoteCursors, updateLocalCursor
3. Remove the existing cursor rendering template (lines 264-269) that references WhiteboardCursorPointer with connectedUsers
4. Replace with new template that iterates remoteCursors Map:
   - Use v-for="[clientId, cursor] in remoteCursors" :key="clientId"
   - Render WhiteboardCursorPointer only if cursor?.cursor exists (has position)
   - Pass the cursor object (which contains user + cursor + tool) as presence prop
5. In handleMouseMove function, call updateLocalCursor(pos.x, pos.y, currentTool.value)
   - Use raw stage coordinates (not viewport-adjusted) for cursor position
6. Add cleanup call in onUnmounted hook to clear cursor on disconnect

NOTE: Template already has ClientOnly wrapper for cursors - keep that pattern.
NOTE: connectedUsers prop will be replaced by remoteCursors from useCursors.
</action>
  <verify>
WhiteboardCanvas.vue calls useCursors(), renders remote cursors via WhiteboardCursorPointer, and calls updateLocalCursor on mousemove.
</verify>
  <done>
WhiteboardCanvas integrates useCursors composable, displays remote user cursors, and broadcasts local cursor position.
</done>
</task>

</tasks>

<verification>
After implementation:
1. Open two browser windows to same whiteboard session
2. Move cursor in one window
3. Verify cursor appears in other window with correct color and name
4. Verify cursor does NOT appear in local window (own cursor)
5. Verify clicking through cursor area still selects canvas elements (listening: false equivalent)
</verification>

<success_criteria>
1. useCursors composable created with Awareness API integration
2. WhiteboardCursorPointer component renders colored cursor with name label
3. WhiteboardCanvas integrates useCursors and renders remote cursors
4. Local cursor broadcasts on mousemove (throttling in 05-04)
5. Remote cursors render with pointer-events-none (no hit detection interference)
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-time-collaboration/05-01-SUMMARY.md`
</output>
