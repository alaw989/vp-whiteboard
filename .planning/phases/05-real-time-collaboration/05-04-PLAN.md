---
phase: 05-real-time-collaboration
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - composables/useCursors.ts
  - composables/useDrawingTools.ts
  - composables/useCollaborativeCanvas.ts
autonomous: true

must_haves:
  truths:
    - "Cursor updates throttled to ~60fps (16ms minimum)"
    - "Network tab shows reduced WebSocket messages during mouse movement"
    - "Cursor movement remains smooth despite throttling"
    - "Active stroke points throttled during drawing (optional optimization)"
    - "Stale cursors auto-removed after 30 seconds"
  artifacts:
    - path: "composables/useCursors.ts"
      provides: "Throttled cursor updates using RAF pattern"
      exports: ["updateLocalCursor with RAF throttling"]
      min_lines: 100
  key_links:
    - from: "composables/useCursors.ts"
      to: "@vueuse/core useDebounceFn"
      via: "Import for RAF throttling"
      pattern: "useDebounceFn|requestAnimationFrame"
    - from: "composables/useCursors.ts"
      to: "awareness.setLocalState"
      via: "Throttled update function"
      pattern: "awareness\\.setLocalState.*throttle|rafId"
---

<objective>
Implement cursor and stroke throttling for performance optimization.

Purpose: Prevent excessive network traffic from rapid cursor movement and stroke broadcasting while maintaining smooth UX. Uses requestAnimationFrame throttling (~60fps) and Yjs Awareness automatic cleanup.

Output: Reduced WebSocket message frequency with smooth cursor motion.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-real-time-collaboration/05-RESEARCH.md
@/home/deck/Sites/vp-whiteboard/composables/useCursors.ts
@/home/deck/Sites/vp-whiteboard/composables/useCollaborativeCanvas.ts
@/home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCanvas.vue
</context>

<tasks>

<task type="auto">
  <name>Add RAF throttling to updateLocalCursor</name>
  <files>composables/useCursors.ts</files>
  <action>
Modify /home/deck/Sites/vp-whiteboard/composables/useCursors.ts:

1. Import useDebounceFn from @vueuse/core
2. Create throttled version of updateLocalCursor:
   - Wrap awareness.setLocalState call in useDebounceFn
   - Set debounce delay to 16ms (~60fps)
   - OR use requestAnimationFrame pattern:
     * Let rafId: number | null = null
     * In updateLocalCursor, if rafId !== null, return (already scheduled)
     * Set rafId = requestAnimationFrame(() => {
         awareness.setLocalState({ user: currentUser.value, cursor: {x, y}, tool })
         rafId = null
       })
3. Ensure last call wins (debounce behavior, not throttle)
   - This means if multiple calls happen within 16ms, only last executes
4. Test that cursor still appears smooth

ALTERNATIVE: Use VueUse's useDebounceFn which already handles RAF-style timing:
   const updateLocalCursor = useDebounceFn((x: number, y: number, tool?: DrawingTool) => {
     awareness.setLocalState({ user: currentUser.value, cursor: {x, y}, tool })
   }, 16)
</action>
  <verify>
useCursors.ts updateLocalCursor function uses RAF or useDebounceFn with 16ms delay.
</verify>
  <done>
Cursor updates throttled to ~60fps using requestAnimationFrame pattern.
</done>
</task>

<task type="auto">
  <name>Add Awareness auto-cleanup documentation</name>
  <files>composables/useCursors.ts</files>
  <action>
Modify /home/deck/Sites/vp-whiteboard/composables/useCursors.ts:

1. Add comment explaining Awareness automatic cleanup:
   - Awareness automatically removes users on disconnect
   - 30-second timeout for stale connections
   - No manual cleanup needed for remote cursors
2. Ensure cleanup() function still calls awareness.setLocalState(null)
   - This marks CURRENT user as offline
   - Remote users handled automatically by Awareness
3. Add JSDoc comment explaining the 30-second inactivity filter in remoteCursors

NOTE: Awareness API handles timeout automatically. The 30-second filter in remoteCursors is for additional safety, but Awareness should handle it.
</action>
  <verify>
useCursors.ts has comments explaining Awareness automatic cleanup behavior.
</verify>
  <done>
Cursor composable documents Awareness automatic timeout behavior.
</done>
</task>

<task type="auto">
  <name>Add optional stroke point throttling</name>
  <files>composables/useCollaborativeCanvas.ts</files>
  <action>
Modify /home/deck/Sites/vp-whiteboard/composables/useCollaborativeCanvas.ts:

1. In broadcastStrokePoint function, add optional throttling:
   - Create lastBroadcastTime map: Map<string, number>
   - Check: if (Date.now() - (lastBroadcastTime.get(strokeId) || 0) < 16) return
   - Update: lastBroadcastTime.set(strokeId, Date.now())
2. This prevents broadcasting more than ~60 points per second per stroke
3. Alternative: Throttle based on point count:
   - Only broadcast every Nth point (e.g., every 3rd point)
   - Interpolate between points on receiving end for smoothness
4. Add STROKE_THROTTLE_MS constant (16) at top of file
5. Document the throttling strategy in JSDoc

DECISION: Use time-based throttling (16ms) rather than count-based. This provides consistent real-time feel regardless of drawing speed.
</action>
  <verify>
useCollaborativeCanvas.ts broadcastStrokePoint implements 16ms throttling check.
</verify>
  <done>
Stroke point broadcasting throttled to prevent excessive network traffic during fast drawing.
</done>
</task>

</tasks>

<verification>
After implementation:
1. Open browser DevTools Network tab
2. Filter by WebSocket messages
3. Move cursor rapidly across canvas
4. Verify WebSocket messages are capped at ~60 per second (not every pixel)
5. Draw continuous stroke rapidly
6. Verify stroke points broadcast at reasonable rate (not excessive)
7. Verify cursor still appears smooth to remote users
8. Wait 30+ seconds with one window inactive
9. Verify that user's cursor disappears or is filtered from list
</verification>

<success_criteria>
1. updateLocalCursor uses 16ms RAF/debounce throttling
2. Network traffic reduced during rapid cursor movement
3. Cursor motion remains smooth (no visible stuttering)
4. Stroke point throttling implemented (16ms minimum)
5. Awareness automatic cleanup documented
6. No manual cursor timeout code needed (Awareness handles it)
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-time-collaboration/05-04-SUMMARY.md`
</output>
