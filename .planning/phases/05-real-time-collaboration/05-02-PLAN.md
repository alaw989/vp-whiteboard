---
phase: 05-real-time-collaboration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/whiteboard/UserPresenceList.vue
  - pages/whiteboard/[id].vue
autonomous: true

must_haves:
  truths:
    - "User list displays all participants in current session"
    - "Each user shows color indicator, name, and current tool"
    - "Online status indicator shows green for active (5s), gray for idle (30s)"
    - "User count badge shows total active users"
    - "Users inactive for 30+ seconds are filtered from list"
  artifacts:
    - path: "components/whiteboard/UserPresenceList.vue"
      provides: "Floating user presence list component"
      min_lines: 60
  key_links:
    - from: "pages/whiteboard/[id].vue"
      to: "components/whiteboard/UserPresenceList.vue"
      via: "Component import and template usage"
      pattern: "UserPresenceList.*remoteCursors|connectedUsers"
---

<objective>
Create floating user presence list showing all session participants.

Purpose: Provide visual awareness of who is in the session, their assigned colors, and their current tool selection. This builds on the cursor tracking from 05-01 to show persistent user information.

Output: Floating panel in top-right corner displaying all active users.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-real-time-collaboration/05-RESEARCH.md
@/home/deck/Sites/vp-whiteboard/components/whiteboard/WhiteboardCanvas.vue
@/home/deck/Sites/vp-whiteboard/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create UserPresenceList component</name>
  <files>components/whiteboard/UserPresenceList.vue</files>
  <action>
Create new component at /home/deck/Sites/vp-whiteboard/components/whiteboard/UserPresenceList.vue:

1. Define props:
   - users: Map<number, any> (the remoteCursors Map from useCursors)
   - currentUser: { id, name, color } (for showing current user too)
2. Create computed userList:
   - Convert Map to Array via Array.from(users.values())
   - Filter: only include users seen within 30000ms (30 seconds)
   - Sort alphabetically by name
3. Create computed allUsers:
   - Combine currentUser with filtered userList
   - This ensures current user appears in list too
4. Create computed userCount: return allUsers.length
5. Create computed activeCount: users with lastSeen < 5000ms (5 seconds)
6. Template structure:
   - Fixed position div: class="fixed top-4 right-4 bg-white rounded-lg shadow-lg p-3 max-w-xs z-50"
   - Header: "In Session" with badge showing userCount
   - List: v-for="user in allUsers" :key="user.id"
   - Each list item:
     * Color indicator: div class="w-3 h-3 rounded-full" :style="{ backgroundColor: user.color }"
     * User info: name (text-sm font-medium), tool (text-xs text-gray-500)
     * Online status: w-2 h-2 rounded-full with bg-green-500 (active) or bg-gray-300 (idle)
   - Tool display: show user.tool or 'Viewing' if no tool
   - Use Tailwind classes for all styling

IMPORTANT: Add transition for smooth list updates when users join/leave.
</action>
  <verify>
File /home/deck/Sites/vp-whiteboard/components/whiteboard/UserPresenceList.vue exists and renders user list with color indicators and status.
</verify>
  <done>
UserPresenceList component renders floating panel showing all active users with color, name, tool, and online status.
</done>
</task>

<task type="auto">
  <name>Integrate UserPresenceList in whiteboard page</name>
  <files>pages/whiteboard/[id].vue</files>
  <action>
Modify /home/deck/Sites/vp-whiteboard/pages/whiteboard/[id].vue:

1. Import UserPresenceList component
2. In the WhiteboardCanvas usage area:
   - Find where useCursors is called
   - Destructure currentUser from useCursors return
   - Pass remoteCursors and currentUser to UserPresenceList
3. Add UserPresenceList component to template:
   - Place after WhiteboardCanvas component
   - Bind :users="remoteCursors" and :current-user="currentUser"
4. Ensure component renders outside the canvas container (sibling, not child)

ALTERNATIVE: If WhiteboardCanvas has a slot for overlays, use that instead.
</action>
  <verify>
pages/whiteboard/[id].vue imports and renders UserPresenceList with remoteCursors and currentUser props.
</verify>
  <done>
UserPresenceList integrated into whiteboard page displaying all session participants.
</done>
</task>

</tasks>

<verification>
After implementation:
1. Open whiteboard in browser
2. Verify user list appears in top-right corner
3. Open second browser window to same session
4. Verify user count increases to 2
5. Verify both users appear with correct colors
6. Wait 30 seconds without activity in one window
7. Verify inactive user disappears from list (or shows idle status)
</verification>

<success_criteria>
1. UserPresenceList component created with floating panel UI
2. User list shows all active users with color indicators
3. Online status indicator works (green/gray)
4. Tool display shows current tool or 'Viewing'
5. Component integrated into whiteboard page
6. 30-second inactivity filter works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-time-collaboration/05-02-SUMMARY.md`
</output>
