---
phase: 08-performance-mobile
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/whiteboard/WhiteboardToolbar.vue
  - pages/whiteboard/[id].vue
autonomous: true

must_haves:
  truths:
    - "UI is responsive on mobile screens (< 640px width)"
    - "Toolbar collapses to bottom sheet on mobile devices"
    - "Primary tools are accessible via bottom toolbar strip"
    - "All tools remain accessible through expandable menu"
    - "Touch targets meet minimum 44x44px size"
    - "Canvas remains visible when toolbar is collapsed"
  artifacts:
    - path: "components/whiteboard/WhiteboardToolbar.vue"
      provides: "Responsive toolbar with mobile bottom sheet"
      min_lines: 350
    - path: "pages/whiteboard/[id].vue"
      provides: "Mobile layout container"
      contains: "md:hidden"
  key_links:
    - from: "components/whiteboard/WhiteboardToolbar.vue"
      to: "pages/whiteboard/[id].vue"
      via: "component props and emit events"
      pattern: "@select-tool|@select-color|@select-size"
---

<objective>
Create a responsive UI that adapts to mobile screens with a bottom sheet toolbar layout.

Purpose: The current vertical sidebar toolbar does not work well on mobile devices where screen space is limited and touch interaction requires larger touch targets. A bottom sheet pattern provides thumb-zone accessibility and maximizes canvas visibility.

Output: Responsive WhiteboardToolbar component with bottom sheet on mobile, collapsed state with primary tool strip, and expanded state with full tool palette.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-mobile/08-RESEARCH.md

# Reference current toolbar structure
@components/whiteboard/WhiteboardToolbar.vue
</context>

<tasks>

<task type="auto">
  <name>Create responsive toolbar with bottom sheet layout</name>
  <files>components/whiteboard/WhiteboardToolbar.vue</files>
  <action>
    Modify WhiteboardToolbar.vue to implement responsive layout:

    1. Add mobile state tracking:
       - isMobile (computed from window.innerWidth < 640px)
       - toolbarExpanded (ref, defaults to false on mobile)
       - primaryTools array (select, pen, highlighter, pan, measure-distance)

    2. Desktop sidebar (existing behavior):
       - Add "hidden md:flex" classes to existing toolbar wrapper
       - Keep all existing functionality intact

    3. Mobile bottom sheet structure:
       - Add "md:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-neutral-200 z-20"
       - Add env(safe-area-inset-bottom) padding for notched phones

    4. Collapsed state (toolbarExpanded = false):
       - Horizontal flex strip with primary tool buttons
       - Minimum 44x44px touch targets (w-11 h-11 = 44px)
       - Chevron-up icon button to expand

    5. Expanded state (toolbarExpanded = true):
       - Vertical scrollable container with max-h-80
       - All tools, colors, sizes in vertical layout
       - Chevron-down icon button to collapse
       - Semi-transparent backdrop (optional)

    6. Close expanded toolbar when tool is selected (auto-collapse for better UX)

    7. Add responsive classes for touch targets:
       - Desktop: p-2 (32px)
       - Mobile: p-3 or w-11 h-11 (44px minimum)

    Do NOT change the tool selection logic, color/size pickers, or emit events.
  </action>
  <verify>
    1. Check mobile layout appears at < 640px viewport
    2. Verify toolbar expands/collapses with chevron button
    3. Confirm all tools accessible in expanded state
    4. Validate touch targets are minimum 44x44px
  </verify>
  <done>
    Desktop shows vertical sidebar, mobile shows bottom sheet toolbar that expands to show all tools and collapses after selection.
  </done>
</task>

<task type="auto">
  <name>Update whiteboard page layout for mobile toolbar</name>
  <files>pages/whiteboard/[id].vue</files>
  <action>
    Modify pages/whiteboard/[id].vue to accommodate mobile toolbar:

    1. Update sidebar wrapper classes:
       - Add "hidden md:flex" to left sidebar aside element
       - This hides sidebar on mobile, shows on desktop

    2. Add mobile toolbar container:
       - Add WhiteboardToolbar component outside sidebar
       - Use conditional rendering or v-if based on screen size
       - Or use CSS to show appropriate toolbar variant

    3. Add safe-area padding for mobile:
       - Add pb-16 (padding-bottom) to main content on mobile
       - This ensures canvas isn't covered by collapsed toolbar

    4. Ensure canvas container handles toolbar space:
       - On mobile: canvas has bottom padding for toolbar
       - On desktop: existing layout unchanged

    Use existing WhiteboardToolbar component - it handles responsive display internally.
  </action>
  <verify>
    1. Desktop shows sidebar toolbar on left
    2. Mobile hides sidebar, shows bottom toolbar
    3. Canvas content not obscured by toolbar on either layout
  </verify>
  <done>
    Page layout accommodates both desktop sidebar and mobile bottom sheet toolbar without overlapping canvas content.
  </done>
</task>

</tasks>

<verification>
1. Open whiteboard page on desktop viewport (> 640px) - verify vertical sidebar toolbar
2. Resize browser to mobile width (< 640px) - verify bottom sheet appears
3. Click expand chevron on mobile - verify full tool palette displays
4. Select a tool - verify toolbar collapses after selection
5. Verify all 14 tools accessible on mobile
6. Check touch targets are at least 44x44px
7. Test on actual mobile device or browser dev tools mobile emulation
8. Confirm PERF-03 (Large PDF files load with progress indicator) - this was implemented in Phase 2 Plan 04 (Lazy page loading and progress indicator)
</verification>

<success_criteria>
1. Mobile (< 640px) shows bottom sheet toolbar with collapsed strip of primary tools
2. Expanded toolbar displays all tools in scrollable vertical layout
3. Desktop (> 640px) shows existing vertical sidebar with no changes
4. All tools accessible on mobile with minimum 44x44px touch targets
5. Canvas remains visible with toolbar collapsed
6. Toolbar auto-collapses after tool selection on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/08-performance-mobile/08-06-SUMMARY.md`
</output>
