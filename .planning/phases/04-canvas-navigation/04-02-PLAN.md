---
phase: 04-canvas-navigation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - composables/useViewport.ts
  - components/whiteboard/WhiteboardCanvas.vue
  - components/whiteboard/WhiteboardToolbar.vue
autonomous: true

must_haves:
  truths:
    - "Pan tool toggles stage draggable state"
    - "Drag end captures final viewport position"
    - "Pan tool state doesn't interfere with drawing tools"
    - "Pan can be activated via toolbar button or keyboard shortcut (if added)"
    - "Viewport x/y correctly reflect stage position after pan"
  artifacts:
    - path: "composables/useViewport.ts"
      provides: "Pan state management (isPanning ref)"
      min_lines: 100
      exports: ["isPanning", "enablePan", "disablePan"]
    - path: "components/whiteboard/WhiteboardCanvas.vue"
      provides: "Pan tool integration"
      contains: "startPan|enablePan"
  key_links:
    - from: "components/whiteboard/WhiteboardCanvas.vue"
      to: "composables/useViewport.ts"
      via: "enablePan/disablePan calls during pan tool interaction"
      pattern: "enablePan|disablePan"
    - from: "components/whiteboard/WhiteboardToolbar.vue"
      to: "components/whiteboard/WhiteboardCanvas.vue"
      via: "Pan tool button emits currentTool change"
      pattern: "currentTool.*pan"
---

<objective>
Implement production-quality pan-by-drag functionality. The pan tool should enable Konva's built-in stage dragging, track pan state, and ensure viewport position is synchronized correctly after drag operations. Add pan button to toolbar for easy access.

Purpose: Enable users to navigate large drawings by dragging the canvas. The existing pan tool toggles `draggable: true` but needs proper state tracking and position capture after drag completes.

Output: Fully functional pan tool with toolbar button, state tracking, and correct viewport synchronization.
</objective>

<execution_context>
@.home/deck/.claude/get-shit-done/workflows/execute-plan.md
@.home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-canvas-navigation/04-RESEARCH.md
@.planning/STATE.md

# Reference prior plan
@.planning/phases/04-canvas-navigation/04-01-SUMMARY.md

# Reference existing implementation
@composables/useViewport.ts
@components/whiteboard/WhiteboardCanvas.vue
@components/whiteboard/WhiteboardToolbar.vue
</context>

<tasks>

<task type="auto">
  <name>Add pan state tracking to useViewport composable</name>
  <files>composables/useViewport.ts</files>
  <action>
Extend useViewport composable with proper pan state management:

1. Add isPanning ref:
   ```typescript
   const isPanning = ref(false)
   ```
   - Tracks whether pan mode is currently active
   - Used to prevent other tools from interfering

2. Rename startPan to enablePan with enhanced behavior:
   - Sets isPanning.value = true
   - Calls stage.draggable(true)
   - Updates stage cursor to 'grab'

3. Rename stopPan to disablePan with enhanced behavior:
   - Gets final stage position via stage.position()
   - Updates viewport.value.x and viewport.value.y
   - Sets isPanning.value = false
   - Calls stage.draggable(false)
   - Resets stage cursor to default

4. Add watch for dragmove when panning:
   - Watch stage position changes during active pan
   - Update viewport.value.x/y in real-time
   - Only update when isPanning.value is true
   - Prevents unnecessary updates during other operations

5. Export isPanning, enablePan, disablePan from composable

Follow pattern from useSelection for reactive state tracking.
Reference existing pan behavior in WhiteboardCanvas.vue lines 592-701.
  </action>
  <verify>
1. useViewport exports isPanning (readonly ref)
2. enablePan function sets draggable=true and cursor='grab'
3. disablePan function captures position and sets draggable=false
4. isPanning ref is readonly
  </verify>
  <done>
useViewport has complete pan state management:
- isPanning readonly ref tracks pan state
- enablePan enables drag with grab cursor
- disablePan captures final position and disables drag
- Drag position updates viewport in real-time
  </done>
</task>

<task type="auto">
  <name>Update WhiteboardCanvas pan tool integration</name>
  <files>components/whiteboard/WhiteboardCanvas.vue</files>
  <action>
Update WhiteboardCanvas.vue to use enhanced pan functions from useViewport:

1. Destructure new exports from useViewport (around line 408):
   ```typescript
   const {
     viewport,
     stageConfig,
     handleWheel,
     isPanning,
     enablePan,
     disablePan,
   } = useViewport({
     stageRef,
     containerRef,
     minZoom: 0.1,
     maxZoom: 5.0,
   })
   ```

2. Update handleMouseDown pan tool handling (lines 592-595):
   - Replace `stageConfig.value.draggable = true` with `enablePan()`
   - Keep the early return for pan tool

3. Update handleMouseUp pan handling (lines 693-701):
   - Replace entire pan block with `if (isPanning.value) { disablePan(); return }`
   - Remove manual position capture (now in disablePan)

4. Ensure handleStageClick properly handles pan tool:
   - The existing useSelection composable handles stage clicks
   - Verify no element selection occurs during pan drag
   - Pan clicks should not create elements

5. Add keyboard shortcut for pan tool (optional but recommended):
   - In handleKeyDown, add 'H' key for hand/pan tool
   - Only trigger if not typing in input/textarea

DO NOT break:
- Drawing tool mouse handlers (pen, highlighter, etc.)
- Selection tool behavior
- Element drag handlers
  </action>
  <verify>
1. Pan tool calls enablePan/disablePan from useViewport
2. isPanning guards prevent other operations during pan
3. Pan tool doesn't interfere with element selection
4. Drawing tools work correctly after pan
5. Run `npm run typecheck` - no errors
  </verify>
  <done>
WhiteboardCanvas pan tool fully integrated:
- Uses enablePan/disablePan from composable
- isPanning prevents conflicts with other tools
- Keyboard shortcut 'H' switches to pan tool
- Drawing tools work normally after pan operation
  </done>
</task>

<task type="auto">
  <name>Add pan tool button to WhiteboardToolbar</name>
  <files>components/whiteboard/WhiteboardToolbar.vue</files>
  <action>
Add pan tool button to the toolbar for easy access:

1. Find the toolbar button section in template
2. Add pan tool button with:
   - Icon: Hand/grab icon (Lucide-vue 'hand' icon or SVG)
   - Click handler: Emits tool-change with 'pan'
   - Active state: Highlighted when currentTool === 'pan'
   - Keyboard shortcut hint: 'H'

3. Follow existing button pattern:
   - Same button styling as other tool buttons
   - Rounded background when active
   - Hover state for visual feedback
   - Tool name in title attribute for accessibility

4. Add 'pan' to DrawingTool type if not already present (verify in types/index.ts)
   - It should already be defined

Reference existing tool buttons like select, pen, highlighter for consistent styling.
  </action>
  <verify>
1. Pan button visible in toolbar
2. Pan button has hand icon
3. Clicking pan button activates pan tool (currentTool === 'pan')
4. Pan button shows active state when selected
5. Keyboard shortcut 'H' shows in tooltip or next to button
  </verify>
  <done>
Pan button added to toolbar:
- Hand/grab icon visible
- Click activates pan tool
- Active state highlighted
- Keyboard shortcut 'H' indicated
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Open whiteboard in browser
2. Click pan tool button (or press 'H')
3. Verify cursor changes to 'grab'
4. Drag canvas - viewport moves smoothly
5. Release mouse - dragging stops, viewport position is correct
6. Switch to another tool (e.g., pen)
7. Draw something - verify coordinates are correct
8. Test that element selection still works
9. Test that existing tools (arrow, shapes, etc.) still function
10. Verify no console errors
11. Test rapid pan-draw-pan cycles
</verification>

<success_criteria>
- Pan tool button visible and functional
- Pan drag moves canvas smoothly
- Viewport position correct after pan
- Pan doesn't interfere with drawing tools
- Cursor feedback shows pan mode active
- Keyboard shortcut 'H' switches to pan tool
</success_criteria>

<output>
After completion, create `.planning/phases/04-canvas-navigation/04-02-SUMMARY.md` with:
- Files modified
- Pan behavior test results
- Any UX issues discovered
- Integration notes for Plan 03 (viewport sync)
</output>
