New whiteboard access request

{{ $user->name }} ({{ $user->email }}) has requested access to the VP Associates collaborative whiteboard.

Approve: {{ $approveUrl }}
Deny:    {{ $denyUrl }}

The link opens a confirmation page — sign in as the owner to complete the action.
